apiVersion: batch/v1
kind: Job
metadata:
  name: tsunami-scanner-1.1.1.3
  namespace: kube-system
  labels:
    jobgroup: tsunami-scanner
spec:
  template:
    metadata:
      name: tsunami-scanner
      labels:
        jobgroup: tsunami-scanner
    spec:
      containers:
      - name: scanner
        image: tsunami
        args: ["sh", "-c", "--ip-v4-target=1.1.1.3"]
      restartPolicy: Never