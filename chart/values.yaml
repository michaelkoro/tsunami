# Default values for tsunami.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: tsunami
  pullPolicy: IfNotPresent

namespace: kube-system
serviceName: tsunami-scanner

servers:
 - 1.1.1.1
 - 1.1.1.3
 - 2.2.2.2
 - 100.200.100.200